# Playwright E2E Test Configuration
# Copy this file to .env.test and fill in the values
#
# Authentication Options (choose one):
#
# Option 1: Storage State (Recommended)
# Run `npx playwright test auth.setup.ts --headed` to generate storage state
# The test will save authentication to tests/e2e/.auth/user.json
#
# Option 2: Clerk Session Token
# Get the __session cookie value from your browser dev tools after logging in
# CLERK_SESSION_TOKEN=your-session-token-here
#
# Option 3: DevLogin Key (Requires backend support)
# DEVLOGIN_KEY=your-devlogin-key-here

# Base URL for testing (defaults to dev.abm.dev)
BASE_URL=https://dev.abm.dev

# Override storage state path (optional)
# PLAYWRIGHT_STORAGE_STATE=path/to/custom/state.json
